export class ForYou {
  degree: string;
  level: string;
  progress: string;
  quote: string;
  constructor(degree: string, level: string, progress: string, quote: string) {
    this.degree = degree;
    this.level = level;
    this.progress = progress;
    this.quote = quote;
  }
}
